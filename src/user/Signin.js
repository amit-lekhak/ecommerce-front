import React from "react";
import Layout from "../core/Layout";

const Signin = (props) => (
    <Layout title="Signin Page" description="Signin to React Node e-commerce App">
        ...
    </Layout>
    );

export default Signin;
