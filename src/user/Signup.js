import React from "react";
import Layout from "../core/Layout";
import { API } from "../config";

const Signup = (props) => (
  <Layout title="Signup Page" description="Signup to React Node e-commerce App">
    {API}
  </Layout>
);

export default Signup;
